import { DytDraggableRowDirective } from './dyt-draggable-row.directive';

describe('DytDraggableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DytDraggableRowDirective();
    expect(directive).toBeTruthy();
  });
});
