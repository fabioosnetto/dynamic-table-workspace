import { DytSelectableRowDirective } from './dyt-selectable-row.directive';

describe('DytSelectableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DytSelectableRowDirective();
    expect(directive).toBeTruthy();
  });
});
